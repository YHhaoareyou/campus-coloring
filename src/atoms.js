const currentLoc = atom({
  key: 'currentLoc',
  default: '',
});

const currentImgId = atom({
  key: 'currentImgId',
  default: '',
});

const currentImgSrc = atom({
  key: 'currentImgSrc',
  default: '',
});